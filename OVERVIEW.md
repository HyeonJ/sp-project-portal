# SoftPuzzle PM

고객사와 대행사(프로젝트팀)가 산출물을 주고받으며 컨펌으로 단계를 넘기는 프로젝트 진행 관리 SaaS. 요구사항·IA·디자인·프로토타입을 파일로 올리고, 고객사가 컨펌하면 다음 게이트가 열린다. 개발·테스트·결함까지 한 흐름으로 추적한다.

## 진행 상황

- **개발(14단계)까지 완료** — 요구사항부터 개발까지 풀스택 MVP 구현 완료.
- 남은 단계: 테스트(15) · UAT(16~17) → 최종 검수 컨펌(게이트 18) → Export·운영 전환(19~20).
- 단계별 상세는 `PROGRESS.md` 참조.

## 데모

`docs/prototype/index.html` — 백엔드 없이 동작하는 단일 파일 프로토타입. 상단 역할 토글(팀 / 고객사 / 관리자)로 권한별 화면을 비교할 수 있다.

## 컨펌 게이트 워크플로우

산출물은 정해진 순서의 슬롯에 쌓이고, 각 단계마다 고객사 컨펌 게이트가 걸린다.

```
요구사항 → IA → 디자인 → 프로토타입 → Figma → 개발
```

- **소프트 게이트**: 메뉴 이동·선행 작업은 허용하되, 아직 안 열린 단계에 손대면 "선행 작업 · 게이트 미통과" 경고를 띄운다. 컨펌 순서는 강제, 병렬 작업은 허용.
- **개발은 하드 게이트**: 앞 게이트(요구사항~프로토타입)가 전부 통과돼야 개발 트리거가 동작한다.

## 사용자 구조 (3-tier)

- **프로젝트팀**(대행사): 프로젝트 생성·산출물 업로드·검토 요청·개발/TC/결함 관리.
- **고객사**: 산출물 컨펌/반려, 결함 등록, 진행 현황 조회.
- **관리자**: 계정·전체 프로젝트·감사 로그 관리(프로젝트 내용은 읽기 전용).

MVP는 3-tier 접근 구분만 두고 팀 내부 직무 RBAC는 제외.

## 주요 메뉴

- **대시보드**: 프로젝트 KPI·최근 활동.
- **진행 현황**: 20단계 로드맵(6게이트·8마일스톤 위치 시각화, 읽기 전용).
- **산출물**: 슬롯별 파일 업로드·버전·컨펌, 활동 이력·코멘트·파일 뷰어.
- **개발**: 사전조건 게이트 통과 시 개발 트리거, 실행 이력.
- **TC**: 테스트 케이스 단건 등록·CSV 일괄 가져오기, 상태(통과/실패/대기).
- **결함**: 등록(팀·고객사)·심각도/상태·증거 첨부.
- **설정**: 기본 정보·멤버/초대·Danger Zone.

## 차별점

- **산출물 = 버전 스냅샷 모델**: 한 버전은 슬롯에 올린 파일 묶음 전체의 스냅샷. draft 상태는 자유 편집, 검토 요청 시 잠금(immutable). 컨펌된 산출물에 새 버전을 만들면 이전 컨펌이 자동 무효화되고 활동 이력에 기록.
- **선행 산출물 변경 배지**: 선행(예: 요구사항)이 바뀌면 이를 근거로 컨펌됐던 하류(IA·디자인)에 "검토 권장" 배지가 전파. "검토 완료"로 해소.
- **TC ↔ 결함 양방향 연동**: 실패한 TC에서 바로 결함 등록(연결 TC 자동 지정), 결함 상세에서 연결 TC 확인. 다대다.
- **코멘트/활동 이력 분리**: 코멘트는 버전 스냅샷별, 활동 이력은 시스템 이벤트만 시간순.

### TC ↔ 결함 연동 예시

```
TC-007 "로그인 30분 만료" 실패
  → 결함 등록 (연결 TC = TC-007 자동), 심각도 High, 스크린샷 첨부
  → 상태: 미해결 → 해결완료
  → DEF-012 상세에서 역으로 TC-007 확인
```

## 참고 문서

- 화면 설계서: `docs/screen-design/index.html`
- 요구사항(SRS): `docs/requirements.md` · 데이터 모델: `docs/erd.md` · API: `docs/api-spec.md`
- 코드 실행: `README.md` · 코드 구조: `docs/module-map.md`
