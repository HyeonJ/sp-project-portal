<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.softpuzzle.pm.qa.CodeSequenceMapper">

    <select id="allocate" resultType="int">
        INSERT INTO code_sequence (project_id, entity_type, next_no)
        VALUES (#{projectId}, #{entityType}, 1 + #{count})
        ON CONFLICT (project_id, entity_type)
        DO UPDATE SET next_no = code_sequence.next_no + #{count}
        RETURNING next_no - #{count}
    </select>

</mapper>
